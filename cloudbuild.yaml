timeout: 1200s
steps:
- id: 'setup'
  name: gcr.io/cloud-builders/docker
  entrypoint: bash
  args:
  - -c
  - |
    docker build -t gcr.io/$PROJECT_ID/gcp-test:${SHORT_SHA}
    docker push gcr.io/$PROJECT_ID/gcp-test:${SHORT_SHA}
